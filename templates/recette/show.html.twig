{% extends './base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recette.name }}</td>
            </tr>
            <tr>
                <th>Descriptions</th>
                <td>{{ recette.descriptions }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_home_page_index') }}">back to list</a>

    <a href="{{ path('app_home_page_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recette/_delete_form.html.twig') }}
{% endblock %}
